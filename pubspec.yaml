name: hierarchicalstateexample
description: A new Flutter application.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  provider: ^4.1.0-dev
  state_notifier: ^0.5.0
  flutter_state_notifier: ^0.4.2
  freezed_annotation:

  cupertino_icons: ^0.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  freezed:
  json_serializable:
  build_runner: ^1.0.0

flutter:
  uses-material-design: true